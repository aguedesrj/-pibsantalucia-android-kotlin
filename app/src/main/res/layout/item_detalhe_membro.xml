<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/textViewItemDetalheMembroPhone"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:fontFamily="@font/nunito_bold"
        android:text="TextView"
        android:textSize="@dimen/size_text_item_adapter_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/imageButtonItemDetalheMembroWhatsApp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <View
        android:id="@+id/viewItemDetalheMembroLine"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/color_text_color_info"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageButton
        android:id="@+id/imageButtonItemDetalheMembroWhatsApp"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_marginEnd="32dp"
        android:foreground="@drawable/ic_whatsapp"
        android:background="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="@+id/imageButtonItemDetalheMembroPhone"
        app:layout_constraintEnd_toStartOf="@+id/imageButtonItemDetalheMembroPhone"
        app:layout_constraintTop_toTopOf="@+id/imageButtonItemDetalheMembroPhone" />

    <ImageButton
        android:id="@+id/imageButtonItemDetalheMembroPhone"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:foreground="@android:drawable/stat_sys_phone_call"
        android:foregroundTint="#716060"
        android:background="#FFFFFF"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
